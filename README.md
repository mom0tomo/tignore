# tignore

```
# installation
$ go install github.com/mom0tomo/tignore@latest
or
$ brew tap mom0tomo/tap && brew install tignore

# usage
$ tignore vim macos visualstudiocode

# help
$ tignore help
````
